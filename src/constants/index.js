const PANEL_STATE = {
    UNTOUCHED,
    IN_PROGRESS,
    INVALID,
    VALID
};

const CONTACT_OPTIONS = {
    EMAIL_DAILY,
    EMAIL_WEEKLY,
    TEXT_DAILY,
    TEXT_WEEKLY
}

export {
    PANEL_STATE,
    CONTACT_OPTIONS
}